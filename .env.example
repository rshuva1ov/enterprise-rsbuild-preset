# Scope приватного npm (например @myorg). Нужен для short-name в privateDependencies при нескольких реестрах.
NPM_SCOPE=

# URL приватного npm-реестра. Используется вместе с NPM_SCOPE.
NPM_REGISTRY_URL=

# Токен для приватного реестра. Нужен для update-deps.
NPM_TOKEN=
