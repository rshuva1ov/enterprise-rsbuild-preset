# Токен для доступа к приватному npm-реестру @games-alabuga (GitLab).
# Нужен для update-deps — без него @games-alabuga/ui-kit не обновится.
# GitLab: User Settings → Access Tokens (scope: read_api, read_registry).

NPM_TOKEN=
